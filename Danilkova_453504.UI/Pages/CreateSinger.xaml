<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Danilkova_453504.UI.Pages.CreateSinger"
             Title="New Artist"
             BackgroundColor="#121212"
             Shell.BackgroundColor="#121212"
             Shell.ForegroundColor="White">

    <Shell.TitleView>
        <Grid ColumnDefinitions="Auto, Auto, *" Padding="10,0,20,0" ColumnSpacing="20">
            <Label Grid.Column="2" 
                   Text="MUSIC" 
                   TextColor="White"
                   FontAttributes="Bold"
                   FontSize="18"
                   VerticalOptions="Center"
                   HorizontalOptions="End" 
                   CharacterSpacing="2"/>
        </Grid>
    </Shell.TitleView>

    <ScrollView>
        <VerticalStackLayout Padding="25" Spacing="20">

            <Label Text="ADD NEW SINGER" 
                   TextColor="#1DB954" 
                   FontSize="12" 
                   FontAttributes="Bold" 
                   CharacterSpacing="2"
                   HorizontalOptions="Center"
                   Margin="0,10,0,10"/>

            
            <VerticalStackLayout Spacing="5">
                <Label Text="Artist Name" TextColor="#8E8E93" FontSize="13" Margin="5,0"/>
                <Border BackgroundColor="#252525" StrokeThickness="0" StrokeShape="RoundRectangle 10" Padding="15,0">
                    <Entry Text="{Binding Name, UpdateSourceEventName=TextChanged}" 
                           Placeholder="Enter name..." 
                           PlaceholderColor="#555555"
                           TextColor="White" />
                </Border>
            </VerticalStackLayout>

            
            <VerticalStackLayout Spacing="5">
                <Label Text="Age" TextColor="#8E8E93" FontSize="13" Margin="5,0"/>
                <Border BackgroundColor="#252525" StrokeThickness="0" StrokeShape="RoundRectangle 10" Padding="15,0">
                    <Entry Text="{Binding Age, UpdateSourceEventName=TextChanged}" 
                           Placeholder="e.g. 25" 
                           Keyboard="Numeric"
                           PlaceholderColor="#555555"
                           TextColor="White" />
                </Border>
            </VerticalStackLayout>

            
            <VerticalStackLayout Spacing="5">
                <Label Text="Country" TextColor="#8E8E93" FontSize="13" Margin="5,0"/>
                <Border BackgroundColor="#252525" StrokeThickness="0" StrokeShape="RoundRectangle 10" Padding="15,0">
                    <Entry Text="{Binding Country, UpdateSourceEventName=TextChanged}" 
                           Placeholder="e.g. USA, UK" 
                           PlaceholderColor="#555555"
                           TextColor="White" />
                </Border>
            </VerticalStackLayout>

            
            <Button Text="SAVE SINGER" 
                    Command="{Binding SaveSingerCommand}" 
                    BackgroundColor="#1DB954" 
                    TextColor="Black"
                    FontAttributes="Bold"
                    CornerRadius="25"
                    HeightRequest="50"
                    Margin="0,30,0,0">
                <VisualStateManager.VisualStateGroups>
                    <VisualStateGroupList>
                        <VisualStateGroup x:Name="CommonStates">
                            <VisualState x:Name="Normal" />
                            <VisualState x:Name="Disabled">
                                <VisualState.Setters>
                                    <Setter Property="Opacity" Value="0.5" />
                                    <Setter Property="BackgroundColor" Value="#444444" />
                                </VisualState.Setters>
                            </VisualState>
                        </VisualStateGroup>
                    </VisualStateGroupList>
                </VisualStateManager.VisualStateGroups>
            </Button>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>